---
name: project-v2-rework
description: "Astrameath (V2 rework, started 2026-07-02) — now the repo's `main` branch; Phases 1–6 done + deployed, Phase 7 polish = path to MVP"
metadata: 
  node_type: memory
  type: project
  originSessionId: 36938563-6386-481b-9db3-cd30ef53781f
---

**SESSION 2026-07-15 — ALL of Stage 1.5 (tasks 1–7) now BUILT, still LOCAL/UNCOMMITTED, prod NOT redeployed.** Marko said "do stage 1.5 first before commit, then I test everything", so tasks 2–7 were implemented on top of task 1's 4 unpushed commits (79872f8…c807815) but NOT committed — the working tree holds all the new work uncommitted. `npm run build` + tsc + eslint all green. **Three migrations were APPLIED to the astrameath DB (wmnqmssdrwtseovjozxm)** and their files are in `supabase/migrations/` (uncommitted): `20260715_session_source_import` (enum +`import`), `20260715_project_level_time` (time_sessions.task_id→nullable, +project_id not-null backfilled, before-trigger `time_session_set_project` keeps project_id synced from the task, `project_time_totals`/`task_time_totals` rewired off the task join, `xp_session_award` now reads stat/no_xp from the session's project, skips `import`, and RECOMPUTES on a duration edit — trigger fires on started_at too), `20260715_profile_personal` (+birthdate/height_cm/weight_kg/bio). types.ts hand-patched to match. DB trigger logic verified via a rolled-back txn (auto project-fill ✓, manual XP ✓, import=0 XP ✓, edit-to-1h recomputes to 60 ✓). What each task shipped: (2) global quick-add modal `quick-add-task.tsx` — `+` in sidebar + mobile top bar, project picker defaults to last-used (localStorage), `useQuickAddTask`; (3) subtasks moved to top of the expanded task panel; (4) project-level Time-log panel `project-time-panel.tsx` (list/add/edit/delete sessions, "Log time" vs "Import (no XP)"), new session hooks in sessions.ts (`useProjectSessions`/`useAddProjectSession`/`useUpdateSession`, `useDeleteSession` now param-less); (5) `about-you.tsx` on Profile + `buildPersonBlock` in snapshot.ts + person block wired into prompt.ts/chat route; (6) `buildProgressSince` in snapshot.ts feeds XP-gained-since-last-chat into the turn — respect is server-owned (`respectGainFromXp`, ~50XP=+1 cap +5, model's respect_delta dropped) and now also gently decays in `applyAbsenceDecay`; affection stays model-driven; (7) NEW CVD-safe 6-stat palette in stats.ts (str#e66767 vit#199e70 int#3987e5 disc#d95926 crea#9085e9 soc#d55181 — validated with the dataviz validator, worst adjacent = str/vit red-green floor pair, always mitigated by glyph+label), applied to per-stat rows + distribution bars + glyphs; stats page gained a per-stat time+XP breakdown, trend vs previous period on the Tracked tile, and active-day streaks (current+longest). `useStatsSessions` fixed to read `project_id` directly (task-less/imported time now counts); `useStatsXpEvents` added. **Next session:** Marko tests everything → fold in his feedback → THEN commit + push + `vercel deploy --prod`. Task 1's real-phone flicker check ([[open-mobile-nav-flicker]]) is still pending too.

**UPDATE 2026-07-14 (folder rename + second MVP round):** The working copy was renamed `~/personal/webdev/projects/habitflow` → **`~/personal/webdev/projects/astrameath`** (memory symlink re-pointed via `.claude/link-memory.sh`; `npm run build` verified green at the new path — node_modules was already the Next 16 install, so the "fresh npm install" TODO was moot and that memory is deleted). Wherever anything below says the code lives in the `habitflow` dir, read `astrameath`. Marko also gave a **second round of feature requests** (Stage 1.5, captured in `docs/plans/mvp-to-launch.md`, nothing built yet): hamburger top-bar nav replacing the mobile bottom bar (supersedes [[open-mobile-nav-flicker]]), global add-task, subtasks to the top of the task panel, project-level time editing + import of old-app hours, optional personal profile data (age/height/weight/bio) fed to the AI, companions' affection/respect bound to XP, stats colour-coded per stat. **Two decisions are settled — don't re-litigate:** imported time awards **no XP**, and the bond link runs **XP → affection/respect, never the reverse** (chatting must not become an XP farm). Stage 1.5 runs **before** Phase 7 polish.

**UPDATE 2026-07-10 (branch promotion):** Astrameath was promoted to be the repo's `main` branch. The old V1 (`main`) branch and its code were **deleted** (recoverable only via reflog commit `4ab27f5` for ~90 days — nothing pushed). The `../astrameath` git worktree was **removed**; astrameath code now lives directly in the primary repo dir `/home/marko/personal/webdev/projects/habitflow` on `main`. A **GitHub remote now exists**: `origin` → `github.com/Marko-Pavlovic-at/astrameath` (private), `main` pushed — this replaces the old "NO git remote exists" reality noted below, so git-based deploy/multi-device now works. V1 app is retired (Marko daily-drives astrameath); V1's Supabase project `ftewcdmoojohaqphhubm` was NOT touched — only the git branch/code was deleted. Everything below is historical build log; where it says "worktree at ../astrameath", "orphan branch", or "no git remote", read it as superseded by this note. Also: memory files are being moved into the repo at `.claude/memory/` + symlinked so they sync across devices (see `.claude/link-memory.sh`).

As of 2026-07-02, Marko is starting a full rework of the habitflow app ("Ascendant") with Fable 5:

- **Goal:** Build a whole new version of the app on a **separate branch**. He will compare it against the old app. Some things stay similar, but he has "improved the general idea."
- **App name: Astrameath.** Requirements typed in from paper on 2026-07-02 and captured in `docs/plans/astrameath-requirements.md` (17 items, rewritten + open questions listed there). Process agreed: clarify open questions first, THEN build. No code before requirements review is done.
- **Old app stays in use:** He uses the current app daily in production. DECIDED (2026-07-02): V2 is a **completely new entity** — must not touch V1's data or infra at all. Creating a **new Supabase project and new Vercel project** for V2 is explicitly fine. No compatibility with V1 storage required.
- **Key decisions (2026-07-02):** TypeScript + real Postgres schema (no KV) — Marko wants "a professional full-stack app" and delegated remaining stack choices to Claude → chosen: Next.js App Router on Vercel, Supabase CLI migrations + RLS, Tailwind, TanStack Query. Community features and module system are post-MVP (module idea: user-created dynamic filter views over tasks — habits = recurring tasks + filters). Main XP from tracked time; per-stat levels compose the general level (derive, never store). Each AI chatbot has its own relationship stats/memories. MVP is **invite-only**: Supabase signups disabled, only Marko's account (manual); AI cost displayed in UI like V1. "Public Prep" (Stripe, enforced AI quotas, security pass, open signup) is post-MVP. Full specs: `docs/plans/astrameath-requirements.md` + `astrameath-plan.md`.

Context: On 2026-07-02 Marko intentionally wiped ALL previous project memory + CLAUDE.md to test Fable 5 capabilities from a fresh start. A new CLAUDE.md was regenerated from scratch via /init (in repo, covers architecture). Do not treat the missing history as data loss — it was deliberate.

Build state (2026-07-02, Phase 1):
- **Code:** orphan branch `astrameath` of the habitflow repo, checked out as a git worktree at `/home/marko/personal/webdev/projects/astrameath` (V1 stays on `main` in the habitflow dir). Next 16 App Router + TS + Tailwind v4; auth gate is `src/proxy.ts` (Next 16 renamed middleware→proxy, export `proxy`/`proxyConfig`).
- **Supabase V2 project:** name `astrameath`, ref `wmnqmssdrwtseovjozxm`, eu-west-1, free tier. Full schema applied via MCP migration `initial_schema`; migration file lives at `supabase/migrations/20260702_initial_schema.sql`. V1's project is `ftewcdmoojohaqphhubm` ("Ascendant") — NEVER write to it.
- **Vercel V2 project:** `markopavlo69-4721s-projects/astrameath`, prod URL https://astrameath.vercel.app (deployed 2026-07-02, auth gate verified). Supabase env vars set for Production; Preview env vars still missing — CLI v54.0.0 has a bug looping on `env add ... preview` even with `--yes` (update CLI or add via dashboard). `ANTHROPIC_API_KEY` not set yet (Phase 6).
- **Phase 1 COMPLETE (2026-07-02):** signups disabled, Marko's account works (he set his own password), login verified on prod. Gotcha learned: in Next 16 `proxy.ts` the matcher must be `export const config` — NOT `proxyConfig` as the vercel plugin skill docs claim; with the wrong name the matcher is ignored and the proxy redirects all static assets to /login (page renders unstyled + dead JS, forms submit natively with no error). Verified against Next source: only `config` is extracted.
- **Phase 2 CONFIRMED WORKING by Marko (2026-07-02, session end):** projects CRUD (stat chips), tasks (status/priority/estimate/notes/filters), start/stop timer (DB-enforced single active session, sets task in_progress, >12h discard), manual time log + per-task session log, global TimerBar. SQL views `task_time_totals`/`project_time_totals` (security_invoker) added in migration `20260702_time_total_views.sql`; hooks in `src/lib/queries/`. Goals/milestones intentionally deferred to Phase 4 (with milestone XP).
- **Phase 3 COMPLETE (2026-07-03):** recurrence (`src/lib/recurrence.ts`: daily/weekly-days/monthly-day, anchored at scheduled_date else created_at, occurrences derived), per-day completions via `task_completions` (recurring rows' checkbox toggles today's completion, optimistic), calendar month/week/day (`src/components/calendar/`), undated sidebar with HTML5 drag-and-drop + date-input fallback, local-TZ date strings in `src/lib/dates.ts`. Verified end-to-end in browser (16 scripted checks, see [[browser-verification-setup]]) and deployed to prod. **Reviewed by Marko 2026-07-03** ("looks good", drag-and-drop works). His feedback shipped same day: calendar view/date in URL params (phone back button retraces month→day), ← Month button, start/stop timer on day-view rows. GOTCHA: shell cwd resets to the habitflow dir between Bash calls — always `cd` into the worktree in the same command as `git commit`/`vercel deploy` (one commit accidentally landed on V1 main and was reset away, nothing pushed).
- **Phase 4 COMPLETE (2026-07-03):** XP awarded/revoked by **DB triggers** (migration `20260703_xp_triggers.sql`: 1 XP/min, 10 completion, 25 milestone, 50 goal; ref_id-exact undo, cascade-safe; `xp_totals` view). Levels derive in `src/lib/xp.ts` (general = Σ stat levels − 5). Reward catalog `src/lib/rewards.ts`; themes swap --accent via `<html data-theme>`. Profile page + goals/milestones UI. Verified in browser (XP math exact incl. revoke), deployed to prod. `unlocks` table intentionally unused (all rewards level-derived). Marko-requested tweaks shipped 2026-07-03: level+XP widget at the bottom of the desktop sidebar (above Sign out) and as the mobile Profile badge; titles extended to level 100 (Lv 100 title = "Astrameath"); **curve retuned to 200+90/level** per his ask (~88,920 XP balanced to general 100 ≈ 1 year at 4h/day; rewards/themes expansion beyond titles deliberately deferred). He's actively using it — Phase 4 considered accepted.
- **Phase 5 COMPLETE (2026-07-03):** stats page (`src/components/stats/stats-view.tsx`). Range filter (week/month/30d/all) → KPI tiles (tracked, avg/day + per-active-day, active days, best day), per-day column chart (weekly buckets past 42 days; hover/focus readout line instead of floating tooltips), per-project distribution rows (share %, active-day frequency, avg/active day; archived projects fold into one row). Pure aggregation in `src/lib/time-stats.ts`; `["stats-sessions"]` query added to sessions.ts TIME_KEYS. Charts single-hue accent by design: the six stat colors FAIL dataviz CVD validation as a categorical palette (worst pair ΔE 5.4) — identity stays in labels/glyphs. Verified end-to-end in browser (seeded temp user, desktop+mobile, all ranges, math checked), deployed to prod. Marko will do proper testing of everything once the app is complete — Phases 4–5 accepted provisionally.
- **Phase 6 COMPLETE (2026-07-04):** AI companions, verified end-to-end locally AND on prod against the real Claude API (create+avatar upload → chat → relationship deltas → memory compression → recall → both reset buttons). V1 architecture carried over: one streamed `claude-haiku-4-5` inference/turn via `/api/ai/chat` (Node route, cookie auth, server-side persistence + server-built app snapshot), `update_state` tool with `tool_choice: auto` (forcing skips text — V1 gotcha held). User-authored joyland-style personas ({{char}}/{{user}} templates); axes affection/trust/respect/amusement/annoyance; `/api/ai/memorize` compresses oldest messages >80 into `companion_memories` (structured output) and DELETES them; `ai_usage` ledger + session/companion/lifetime USD in chat "Bond" panel; resets on Profile. **ANTHROPIC_API_KEY saga:** V1's Vercel copy is `sensitive`-type → `vercel env pull` silently writes EMPTY values (burned an hour); the real key lives in V1's local `.env.local` as `VITE_ANTHROPIC_API_KEY`; now set correctly (verified non-empty + round-tripped) on astrameath `.env.local` + all three Vercel envs. Also: `vercel env add` CLI still hangs in non-TTY at v54.19 — use the REST API (`POST /v10/projects/{p}/env?upsert=true`, token from `~/.local/share/com.vercel.cli/auth.json`).
- **Acceptance testing done (2026-07-05):** Marko's 8 feedback items documented in worktree `docs/plans/testing-feedback-2026-07-05.md` (milestone add-form not discoverable; AI snapshot blind to projects/goals/non-today tasks — confirmed in `snapshot.ts`; AI wanted everywhere via right sidebar; backstory section in character template; no progress-toward-next-level display; Vercel Pro €20/mo worry → considering VPS, undecided; project-level no-XP tag; AI sense of time — plumbing exists in `prompt.ts`, doesn't land). Full product-readiness review in `docs/plans/product-readiness-gaps.md` (22 items: no signup/password reset, AI spend unenforced, no legal/GDPR, no error pages/monitoring/tests, etc.) — **Marko decided 2026-07-05: ALL readiness gaps are post-MVP; priority is the app functioning properly for him alone.**
- **All 7 feedback fixes SHIPPED + deployed to prod 2026-07-05** (commits 6ba8b54, a2419d4, 131ef12; hosting-cost item parked as business decision): level-progress bars (new `nextGeneralLevelUp` in xp.ts — nearest stat level-up = next general level), goal chevron + "+ add milestones" hint, `projects.no_xp` (migration `20260705_project_no_xp.sql`, all 5 award triggers guarded via `*_awards_xp` helpers; only future XP affected), snapshot now has projects/goals/next-7-days/backlog, lived-time response rule + "═══ Time ═══" header, persona.backstory (own prompt block, old rows parse empty), CompanionSidebar right rail ≥1200px on all views except /companions (ChatView `compact` prop). Verified 13/13 in browser incl. live Haiku turn that named seeded projects + correct time of day. GOTCHA: during apply_migration a stray `old.status` line slipped into xp_goal (goals have no status → runtime break); hotfixed via execute_sql same minute and both directions re-verified. Deploys go via `vercel deploy --prod --yes` in the worktree (NO git remote exists).
- **Also shipped 2026-07-05 (same session, Marko follow-ups):** companion sidebar minimizable (» in chat header → floating avatar bubble bottom-right; localStorage via useSyncExternalStore — lint bans setState-in-effect; per-browser, offered profile-sync if he wants) + AI reset fixed to KEEP companions ("Reset companions": wipes chats/state/memories, re-seeds bare state row + greeting like fresh creation; ai_usage kept deliberately). Both browser-verified + deployed. Commits: 131ef12 sidebar, +minimize, 77633dd reset.
- **Remaining to MVP: Phase 7 game-UI polish ONLY** (Hollow Knight/Dark Souls/WuWa theming, animations, PWA install, responsive audit at 375/900/1200). Session ended 2026-07-05 evening at Marko's token limit; he'll daily-drive the app for a few days (expect fresh field-testing feedback ~2026-07-08+), then return for Phase 7 — the clean starting point next session.
- **Context redundancy:** full project context (infra refs, invariants, gotchas, phase status) is also committed in the astrameath worktree's `AGENTS.md` + `docs/plans/*` — so if a session starts in `/home/marko/personal/webdev/projects/astrameath` (where this memory dir does NOT auto-load), everything needed is on disk there. His V2 login is markopavlo96@hotmail.com (password his own, unknown — never reset it without asking; he knows it works).

Tooling setup for the rework:
- **Playwright MCP:** installed by Marko, needed session restart to load — verify it's available in the new session (for responsive UI verification at 375/900/1200px).
- **Supabase MCP:** gave him the command: `claude mcp add --scope project --transport http supabase "https://mcp.supabase.com/mcp?project_ref=<ref>&read_only=true"` + OAuth via /mcp. Unknown if he ran it — check.
